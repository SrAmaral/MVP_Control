version: "3.8"

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3001"

  strapi:
    build:
      context: ./strapi
    ports:
      - "1337:1337"
    volumes:
      - ./strapi:/app
    tty: true
    entrypoint: sh -c "npm install && npm run develop"
